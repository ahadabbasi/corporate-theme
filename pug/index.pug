doctype html
html(lang="fa",dir="rtl")
    include header
    body

        nav(class="navbar navbar-expand-lg navbar-light bg-light")
            a(class="navbar-brand", href="#") محل لگو شما
            button(class="navbar-toggler", type="button", data-toggle="collapse", data-target="#navbarSupportedContent", aria-controls="navbarSupportedContent", aria-expanded="false", aria-label="Toggle navigation")
                span.navbar-toggler-icon
            div.collapse.navbar-collapse#navbarSupportedContent
                ul.navbar-nav.mr-auto.text-right
                    li.nav-item.active
                        a(class="nav-link", href="#") خانه
                            span.sr-only (current)
                    li.nav-item
                        a(class="nav-link", href="#") درباره ما
                    li.nav-item
                        a(class="nav-link", href="#") تماس با ما
                form.form-inline.position-relative
                    input(class="form-control w-100", type="search", placeholder="جستجو", aria-label="Search")
                    button(class="btn btn-link position-absolute", type="submit", style="left:0px;") 
                        i.fas.fa-search
        
        //Slide show
        include slider

        .row
            .col-12.col-lg-6

            .col-12.col-lg-6

        include footer